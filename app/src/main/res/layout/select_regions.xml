<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="5dp">

<TextView
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="20dp"
    android:text="veuiller choisir la Region du semencier"
    android:textStyle="bold"
    android:textColor="@color/colorPrimary"
    android:textAlignment="center"
    android:textSize="8pt"
    android:layout_gravity="center_horizontal" />
    <Spinner
        android:id="@+id/combo_region_province"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:spinnerMode="dropdown"
        android:scrollbars="vertical"
        android:layout_marginTop="20dp"
        android:background="@drawable/spiner_layout"/>
    <Spinner
        android:id="@+id/combo_region_commune"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:spinnerMode="dropdown"
        android:scrollbars="vertical"
        android:layout_marginTop="20dp"
        android:background="@drawable/spiner_layout"/>
    <Spinner
        android:id="@+id/combo_region_colline"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:spinnerMode="dropdown"
        android:scrollbars="vertical"
        android:layout_marginTop="20dp"
        android:background="@drawable/spiner_layout"/>
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp">
        <Button
            android:id="@+id/btn_emission_cancel"
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:text="Annuler"
            android:textColor="#f00"
            android:padding="0dp"
            style="@style/Widget.AppCompat.Button.Borderless.Colored"/>
        <Button
            android:id="@+id/btn_emission_submit"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:layout_alignParentRight="true"
            android:background="@drawable/material_button"
            android:text="Consulter"
            android:textColor=" #fff"
            android:layout_alignParentEnd="true" />
    </RelativeLayout>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

    </RelativeLayout>
</LinearLayout>